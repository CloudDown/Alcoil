# Alcoil

Unmaintained Windows prototype. Source only — binaries are not tracked.

- `scan.py` — Python entry
- `macro.au3` — AutoIt source
- `Alcoil.bat` / `install.bat` — local launchers
- `weapon/` — image assets

Do not commit `.exe` files.
